# Railway Configuration for Backend
# This tells Railway how to run the Express server

[build]
  builder = "NIXPACKS"
  buildCommand = "npm ci"

[deploy]
  startCommand = "npm start"
  healthcheckPath = "/health"
  healthcheckTimeout = 30
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
