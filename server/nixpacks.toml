[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'No build step needed for Express server'"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
